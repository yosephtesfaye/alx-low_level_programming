#include <stdio.h>
/**
 * main - return the argument
 * @argv: array with the arguments
 * @argc: count the size
 * Return: always 0
 */
int main(int argc, char *argv[])
		{

		(void)argc;

		pintf("%s\n", argv[0];
		return (0);
		}
