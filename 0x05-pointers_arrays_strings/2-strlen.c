#include "main.h"
#include <string.h>
/**
 * _strlen - count the lengh of a sting
 * @s: takes a parameter
 * Return: int value of lenth
 */
int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
